<UserControl x:Class="Questionnaire.Views.QuestionCheckView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Questionnaire.Views"
        mc:Ignorable="d"
        Height="Auto" Width="Auto">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Twój ulubiony kolor" Grid.Row="0" Margin="10,10,0,0" FontFamily="Trebuchet MS" FontSize="15" VerticalAlignment="Top"/>
            <StackPanel Grid.Row="1" >
                <StackPanel.Resources>
                    <Style TargetType="{x:Type CheckBox}">
                        <Setter Property="Margin" Value="0,10,0,0"/>
                    </Style>
                </StackPanel.Resources>
                <CheckBox Content="Niebieski"/>
                <CheckBox Content="Żółty"/>
                <CheckBox Content="P"/>
                <CheckBox Content="BIały"/>
                <CheckBox Content="ipsum"/>
                <CheckBox Content="Czerwony"/>
                <CheckBox Content="C"/>
            </StackPanel>
    </Grid>
</UserControl>
